<%= form_for [@foodpref] do |f| %>
  <% if @foodpref.errors.any? %>
    <div id="error_explanation">
    <h2><%= pluralize(@foodpref.erros.count, "error") %> *Por favor confira os campos obrigatórios!</h2>
    <ul>
      <% @foodpref.erros.full_messages.each do |message| %>
        <li> <%= message %></li>
      <% end %>
    </ul>  
  </div>  
<% end %>

<p>
  <div class="field">
    <label >Tipos de comida preferida:</label>
    <%= f.text_field :name, class:"form-control"%>
  </div>
</p>

<p>
  <button type="submit">Salvar</button>
</p>

<% end %>